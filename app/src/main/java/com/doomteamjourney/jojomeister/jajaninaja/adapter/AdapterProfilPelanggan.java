package com.doomteamjourney.jojomeister.jajaninaja.adapter;

/*
public class AdapterProfilPelanggan extends RecyclerView.Adapter<AdapterProfilPelanggan.HolderProfilPelanggan> {
    private List<ModelProfilPelanggan> listItem;
    private Context mContext;

    AdapterProfilPelanggan(List<ModelProfilPelanggan> listItem,Context mContext){
        this.listItem = listItem;
        this.mContext = mContext;
    }

    @Override
    public HolderProfilPelanggan onCreateViewHolder(ViewGroup parent, int i) {
        View layout = LayoutInflater.from(parent.getContext()).inflate(R.layout.activity_profil,parent,false);
        HolderProfilPelanggan holderP = new HolderProfilPelanggan(layout);
        return holderP;
    }

    public void onBindViewHolder(HolderProfilPelanggan holder, int position) {
        ModelProfilPelanggan mp = listItem.get(position);
        holder.txNamaMenu.setText(mk.getNama_menu());
        holder.txHarga.setText("Rp. " + mk.getHarga_menu());
        //Glide.with(mcontext).load(mtm.getFoto()).thumbnail(0.5f).crossFade().diskCacheStrategy(DiskCacheStrategy.ALL).into(holder.gbrMenu);
        holder.mk = mk;
    }
}*/
